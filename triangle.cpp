#include<iostream>
using namespace std;
int main(){
    double length, breadth, area;
    
    cout<<"Enter the length of the triangle";
    cin >> length;
    cout<<"Enter the breadth of the triangle";
    cin >> breadth;
    area = 0.5*length*breadth;
    cout << "The Area of " << length << " and " << breadth << " is: " << area << endl;





    return 0;
}